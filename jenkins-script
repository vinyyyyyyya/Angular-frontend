pipeline {
  agent none
  stages {
    stage('Front-end') {
      agent {
        docker { image 'node:16-alpine' }
      }
      steps {
        dir('Angular-frontend') {
                    sh 'npm install'
                    sh 'npm run build'
                }
      }
    }
  }
}
