pipeline {
  agent none
  stages {
    stage('Front-end') {
      agent {
        docker { image 'node:18-alpine' }
      }
      steps {
        dir('angular-frontend') {
                    // Install dependencies and build Angular app
                    sh 'npm install'
                    sh 'npm run build'
                }
      }
    }
  }
}
